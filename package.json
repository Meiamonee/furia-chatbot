{
  "name": "furia-chatbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve-backend": "cd backend && node server.js",
    "serve-frontend": "cd frontend && npm start",
    "start": "concurrently \"npm:serve-backend\" \"npm:serve-frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "openai": "^4.96.2"
  }
}
